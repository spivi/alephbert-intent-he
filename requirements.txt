# These are the versions I tested with (Python 3.12+). Newer versions usually
# work, but ML dependencies move fast, so pin these if a future release breaks.
transformers>=5.9.0
torch>=2.12.0
datasets>=4.8.5
scikit-learn>=1.8.0
onnx>=1.21.0
onnxruntime>=1.24.4
onnxscript>=0.7.0
httpx>=0.28.1
numpy>=2.4.4
matplotlib>=3.10.9
